package util;

import java.util.List;

public class SysUtils {
    public static void prints(List<String> strings,String model){
        String model0 = "OneManyOne";
        String model1 = "TopLineManyDownLine";

        if(model.equals(model0)){
           // String manylength = strings.get(1).l;
        }
    }
}
